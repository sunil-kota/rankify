import 'package:flutter/material.dart';

class Myexams extends StatelessWidget {
  const Myexams({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("This is Myexams page"),
    );
  }
}