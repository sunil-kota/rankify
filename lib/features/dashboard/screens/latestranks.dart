import 'package:flutter/material.dart';

class Latestranks extends StatelessWidget {
  const Latestranks({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("This is Latestranks page"),
    );
  }
}